import React from 'react';
import StyledButton from './components/StyledButton'; // Adjust the import path based on your project's structure

const App = () => {
    return (
        <div className="App">
            <h1>Hello Styled Components!</h1>
            <StyledButton>Click Me!</StyledButton>
        </div>
    );
};

export default App;